# 🎯 转写历史记录功能实现说明

## 📋 **用户需求**

用户要求实现语音转文字内容的历史记录功能：

1. **实时保存**：语音转文字的内容应该进行实时保存
2. **存储位置**：存在软件根目录下的 `audio_text` 文件夹下
3. **记录完整信息**：记录下所有转写信息，包括时间戳、内容、总结状态等
4. **历史载入**：支持用户选择打开一个记录，载入历史记录
5. **载入按钮**：在实验、录音、停止录音按键最右边增加载入按键
6. **载入到面板**：点击载入按钮后选择文件载入到转写面板

## ✅ **完成的功能实现**

### **1. 载入按钮添加** ✅

**按钮位置**：在录音控制区域的最右边
```html
<!-- 载入历史记录按钮 -->
<button id="load-transcript-btn" class="w-12 h-12 bg-purple-500 text-white rounded-lg hover:bg-purple-600 flex items-center justify-center shadow-md transition-all" onclick="loadTranscriptHistory()" title="载入历史记录">
    <span class="material-icons-outlined text-lg">folder_open</span>
</button>
```

**按钮特点**：
- **紫色主题**：与其他按钮区分开来
- **文件夹图标**：直观表示载入功能
- **悬停效果**：提供视觉反馈
- **工具提示**：显示"载入历史记录"

### **2. 转写会话管理** ✅

**会话数据结构**：
```javascript
let currentTranscriptSession = {
    sessionId: null,        // 会话唯一标识
    startTime: null,        // 开始时间
    transcripts: [],        // 转写记录数组
    filePath: null          // 保存文件路径
};

// 转写记录结构
{
    timestamp: "[HH:MM:SS]",    // 显示时间戳
    text: "转写内容",           // 转写文字
    time: "ISO时间字符串",      // 精确时间
    summarized: false           // 是否已总结
}
```

### **3. 实时保存机制** ✅

**自动保存触发**：
```javascript
function addTranscriptText(text) {
    // ... 添加到UI ...
    
    // 添加到当前会话记录
    const transcriptRecord = {
        timestamp: timeString,
        text: text,
        time: new Date().toISOString(),
        summarized: false
    };
    currentTranscriptSession.transcripts.push(transcriptRecord);
    
    // 实时保存到文件
    saveTranscriptSession();
}
```

**保存函数**：
```javascript
function saveTranscriptSession() {
    if (currentTranscriptSession.transcripts.length === 0) return;
    
    if (window.bridge && window.bridge.saveTranscriptSession) {
        const sessionData = {
            sessionId: currentTranscriptSession.sessionId,
            startTime: currentTranscriptSession.startTime,
            transcripts: currentTranscriptSession.transcripts,
            saveTime: new Date().toISOString()
        };
        
        window.bridge.saveTranscriptSession(JSON.stringify(sessionData), function(filePath) {
            if (filePath) {
                currentTranscriptSession.filePath = filePath;
                debugLog('转写会话已保存到: ' + filePath);
            }
        });
    }
}
```

### **4. 会话生命周期管理** ✅

**开始新会话**：
```javascript
function startNewTranscriptSession() {
    const now = new Date();
    currentTranscriptSession = {
        sessionId: 'transcript_' + now.getTime(),
        startTime: now.toISOString(),
        transcripts: [],
        filePath: null
    };
    debugLog('开始新的转写会话: ' + currentTranscriptSession.sessionId);
}
```

**会话触发时机**：
- 用户点击"开始录音"时自动创建新会话
- 每次新的转写内容都会触发保存
- 总结操作会更新记录的总结状态

### **5. 历史记录载入功能** ✅

**载入触发函数**：
```javascript
window.loadTranscriptHistory = function() {
    debugLog('载入历史转写记录');
    
    if (window.bridge && window.bridge.loadTranscriptHistory) {
        window.bridge.loadTranscriptHistory(function(result) {
            if (result) {
                try {
                    const sessionData = JSON.parse(result);
                    loadTranscriptSessionData(sessionData);
                    debugLog('历史记录载入成功');
                } catch (error) {
                    debugError('解析历史记录失败: ' + error.message);
                    alert('载入历史记录失败：文件格式错误');
                }
            } else {
                debugLog('用户取消载入或没有选择文件');
            }
        });
    } else {
        alert('载入功能不可用');
    }
};
```

### **6. 界面数据载入** ✅

**载入会话数据**：
```javascript
function loadTranscriptSessionData(sessionData) {
    // 清空当前转写内容
    const transcriptDiv = document.getElementById('transcript-content');
    transcriptDiv.innerHTML = '';
    
    // 更新当前会话信息
    currentTranscriptSession = {
        sessionId: sessionData.sessionId,
        startTime: sessionData.startTime,
        transcripts: sessionData.transcripts || [],
        filePath: null
    };
    
    // 重新显示所有转写内容
    sessionData.transcripts.forEach(record => {
        addTranscriptItemToUI(record.timestamp, record.text, record.summarized);
    });
    
    debugLog('载入了 ' + sessionData.transcripts.length + ' 条转写记录');
}
```

### **7. UI重建功能** ✅

**不触发保存的UI添加**：
```javascript
function addTranscriptItemToUI(timeString, text, isSummarized = false) {
    // 创建转写条目（与正常添加相同的UI）
    // 支持多选功能
    // 如果已总结，显示√标记
    // 不触发保存操作
}
```

## 🎯 **文件存储格式**

### **JSON文件结构**
```json
{
    "sessionId": "transcript_1727251200000",
    "startTime": "2025-09-25T08:00:00.000Z",
    "transcripts": [
        {
            "timestamp": "[08:00:15]",
            "text": "今天我们来学习柯基的行为习惯",
            "time": "2025-09-25T08:00:15.123Z",
            "summarized": false
        },
        {
            "timestamp": "[08:00:28]",
            "text": "柯基犬需要大量的运动",
            "time": "2025-09-25T08:00:28.456Z",
            "summarized": true
        }
    ],
    "saveTime": "2025-09-25T08:01:00.000Z"
}
```

### **文件命名规则**
- 格式：`transcript_YYYYMMDD_HHMMSS.json`
- 示例：`transcript_20250925_080000.json`
- 位置：`audio_text/transcript_20250925_080000.json`

## 🔧 **后端API需求**

### **需要实现的后端方法**

1. **saveTranscriptSession(sessionData, callback)**
   - 功能：保存转写会话到文件
   - 参数：JSON格式的会话数据
   - 返回：保存的文件路径

2. **loadTranscriptHistory(callback)**
   - 功能：打开文件选择对话框，载入历史记录
   - 参数：无
   - 返回：选中文件的JSON内容

### **文件操作要求**
- 创建 `audio_text` 文件夹（如果不存在）
- 支持JSON文件的读写操作
- 文件选择对话框过滤`.json`文件
- 错误处理和异常捕获

## 🎨 **用户操作流程**

### **保存流程**
1. **开始录音** → 自动创建新的转写会话
2. **实时转写** → 每条转写内容自动保存到文件
3. **AI总结** → 更新记录的总结状态并保存
4. **停止录音** → 完整的会话记录已保存

### **载入流程**
1. **点击载入按钮** → 打开文件选择对话框
2. **选择历史文件** → 从 `audio_text` 文件夹选择
3. **解析并载入** → 清空当前内容，显示历史记录
4. **恢复状态** → 包括已总结标记、多选功能等

### **界面效果**
```
录音控制区：
[🧪测试] [🔴录音] [⏹停止] [📁载入] ← 新增载入按钮

转写面板：
[08:00:15] ✓ 今天我们来学习柯基...  ← 已总结标记
[08:00:28]   柯基犬需要大量运动...   ← 未总结
[08:00:41] ✓ 饮食控制很重要...      ← 已总结标记
```

## 🎉 **功能完成状态**

### **✅ 已实现的功能**
1. ✅ 载入按钮UI添加
2. ✅ 转写会话数据结构设计
3. ✅ 实时保存机制
4. ✅ 会话生命周期管理
5. ✅ 历史记录载入功能
6. ✅ 界面数据重建
7. ✅ 总结状态保持
8. ✅ 错误处理和用户提示

### **🔄 待后端实现**
- `saveTranscriptSession` API方法
- `loadTranscriptHistory` API方法
- `audio_text` 文件夹创建
- JSON文件读写操作

### **🎯 用户体验提升**
- **无感保存**：转写内容自动实时保存，无需手动操作
- **完整记录**：保存所有信息包括时间戳、总结状态等
- **便捷载入**：一键载入历史记录，快速恢复工作状态
- **状态保持**：载入后完全恢复原有的选择和总结状态

---

**实现时间**: 2025-09-25 16:30  
**前端状态**: ✅ 完成  
**后端状态**: 🔄 待实现API支持  
**测试状态**: 🔄 待后端完成后测试

**🎯 转写历史记录功能前端实现完成！现在需要后端API支持即可完整使用！** 🎉✨

**核心功能**：
- ✅ 实时保存转写内容到 `audio_text` 文件夹
- ✅ 载入按钮和文件选择功能
- ✅ 完整的会话数据管理
- ✅ 历史记录界面重建和状态恢复
