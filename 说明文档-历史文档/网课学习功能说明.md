# 网课学习功能说明

## 功能概述

基于 `app_qt.py` 的录音逻辑，在 `overlay_drag_corgi_app.py` 中实现了完整的网课学习页面，包含以下核心功能：

## 主要功能

### 1. Markdown编辑器（左侧）
- ✅ **编辑模式**：支持Markdown语法的文本编辑
- ✅ **预览模式**：实时渲染Markdown内容
- ✅ **模式切换**：点击按钮在编辑和预览模式间切换
- ✅ **光标位置跟踪**：记录光标位置用于插入AI总结

### 2. 实时转写区域（右侧）
- ✅ **实时显示**：录音时实时显示语音转文字结果
- ✅ **文字选择**：支持鼠标选择转写文本
- ✅ **清空功能**：一键清空转写内容
- ✅ **滚动显示**：自动滚动到最新内容

### 3. 录音功能
- ✅ **开始/停止录音**：按钮控制录音状态
- ✅ **设备选择**：支持选择音频输入设备
- ✅ **状态显示**：实时显示录音状态
- ✅ **音频分段**：自动检测静音并分段处理

### 4. AI总结功能
- ✅ **选中文本总结**：选择转写文本后点击按钮进行AI总结
- ✅ **LLM集成**：使用统一的LLM提供者工厂
- ✅ **智能插入**：总结结果自动插入到笔记光标位置
- ✅ **格式化输出**：总结以Markdown格式插入

### 5. 笔记保存
- ✅ **自动保存**：点击按钮保存完整笔记
- ✅ **时间戳**：自动添加创建时间
- ✅ **文件管理**：保存到 `course_notes` 目录

## 技术实现

### 架构设计
- **严格遵循分层架构原则**：模板与业务逻辑完全分离
- **模板系统**：使用Jinja2模板引擎，HTML代码存放在 `templates/pages/learn_from_audio.html`
- **业务逻辑层**：Python代码仅负责数据处理和API调用，不包含任何HTML代码
- **表示层**：前端界面通过模板系统渲染，支持动态数据绑定

### 前端界面
- 使用 Tailwind CSS 构建响应式布局
- 左右分栏设计，50%-50% 宽度分配
- 实时JavaScript交互，无需页面刷新
- 模板文件位置：`templates/pages/learn_from_audio.html`

### 后端API
- `startRecording()` - 开始录音
- `stopRecording()` - 停止录音
- `summarize_text(text)` - 总结选中文本
- `save_course_notes(content)` - 保存笔记
- `getAudioDevices()` - 获取音频设备列表

### 模板管理
- 使用 `TemplateManager` 类管理模板渲染
- `generate_learn_audio_content()` 方法调用 `render_page_content('learn_from_audio')`
- 支持模板继承和组件化开发

### 录音转写流程
1. **AudioRecorderWorker** - 音频录制线程
   - 实时录音并检测静音
   - 自动分段保存音频文件
   - 发送音频文件到转写队列

2. **TranscriberWorker** - 语音转写线程
   - 支持 Whisper 和 Faster-Whisper
   - 异步处理音频文件
   - 实时发送转写结果到前端

3. **前端更新** - 实时界面更新
   - 通过 QWebChannel 接收转写结果
   - 动态添加转写文本到显示区域
   - 更新录音状态指示器

### LLM集成
- 使用 `llm_provider_factory.py` 统一管理LLM调用
- 支持多种LLM提供者：Ollama、Gemini、DeepSeek、Qwen
- 自动错误处理和重试机制

## 使用流程

### 基本使用步骤
1. **启动应用**：运行 `overlay_drag_corgi_app.py`
2. **进入页面**：点击菜单中的"从音视频学习"
3. **开始录音**：点击"开始录音"按钮
4. **观察转写**：在右侧区域查看实时转写结果
5. **选择总结**：选中转写文字，点击"总结选中内容"
6. **查看结果**：AI总结自动插入到左侧笔记中
7. **保存笔记**：点击"保存笔记"按钮保存完整内容

### 高级功能
- **预览模式**：切换到预览模式查看Markdown渲染效果
- **设备选择**：在设置中选择合适的音频输入设备
- **批量总结**：可以多次选择不同文本进行总结
- **手动编辑**：在AI总结基础上手动完善笔记内容

## 配置要求

### 系统依赖
- Python 3.8+
- PySide6
- PyAudio
- NumPy
- Torch
- Whisper 或 Faster-Whisper

### LLM配置
- 至少配置一个可用的LLM提供者
- 确保API密钥正确配置
- 网络连接正常

### 音频设备
- 可用的音频输入设备（麦克风）
- 推荐使用高质量音频设备以提高转写准确率

## 文件结构

```
course_notes/                 # 保存的笔记文件
├── course_note_20240101_120000.md
└── ...

recorded_audio_segments/      # 录音音频片段
├── segment_1234567890.wav
└── ...

conversations/               # AI对话历史（如果使用）
├── conversation_20240101_120000.json
└── ...
```

## 故障排除

### 常见问题
1. **录音无声音**：检查音频设备选择和权限
2. **转写不准确**：尝试更换Whisper模型大小
3. **LLM调用失败**：检查网络连接和API配置
4. **界面无响应**：检查JavaScript控制台错误

### 调试方法
- 查看控制台日志输出
- 使用 `test_online_course_learning.py` 进行功能测试
- 检查 `logs/` 目录下的详细日志文件

## 扩展功能

### 未来可能的改进
- 支持视频文件上传和转写
- 添加更多Markdown编辑功能（表格、图片等）
- 集成知识点提取和管理
- 支持多语言转写
- 添加转写文本的时间戳标记
- 支持导出多种格式（PDF、Word等）

---

*本功能基于柯基学习小助手框架开发，遵循分层架构设计原则*
