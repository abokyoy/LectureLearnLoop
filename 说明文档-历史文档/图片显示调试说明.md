# 图片显示调试说明

## 问题分析
图片显示失败可能的原因：

### 1. 路径问题
- Windows路径分隔符 `\` vs Unix路径分隔符 `/`
- 相对路径 vs 绝对路径
- file:// 协议格式

### 2. 格式转换问题
- Obsidian格式 `![[filename]]` 转换
- 标准Markdown格式处理
- HTML img标签生成

## 调试步骤

### 步骤1: 检查控制台日志
打开浏览器开发者工具，查看控制台中的调试信息：

1. **Obsidian格式转换**:
   ```
   发现Obsidian格式图片: screenshot_20250926_091553.png
   转换为标准格式: ![screenshot_20250926_091553.png](attachments/screenshot_20250926_091553.png)
   ```

2. **图片路径处理**:
   ```
   处理图片路径: attachments/screenshot_20250926_091553.png
   当前文件路径: E:\LLM\LectureLearnLoop\vault\新建笔记1.md
   文档目录: E:\LLM\LectureLearnLoop\vault
   完整图片路径: E:\LLM\LectureLearnLoop\vault\attachments\screenshot_20250926_091553.png
   文件URL: file:///E:/LLM/LectureLearnLoop/vault/attachments/screenshot_20250926_091553.png
   ```

### 步骤2: 检查文件是否存在
确认图片文件确实存在于预期位置：
- 路径：`E:\LLM\LectureLearnLoop\vault\attachments\screenshot_20250926_091553.png`
- 检查文件是否存在且可访问

### 步骤3: 检查HTML输出
在预览模式下，查看生成的HTML代码：
```html
<img src="file:///E:/LLM/LectureLearnLoop/vault/attachments/screenshot_20250926_091553.png">
```

## 可能的解决方案

### 方案1: 修复路径分隔符处理
已在代码中添加了Windows路径分隔符的兼容处理：
```javascript
const pathSeparator = currentFilePath.includes('\\') ? '\\' : '/';
const fullImagePath = docDir + pathSeparator + src.replace(/\//g, pathSeparator);
```

### 方案2: 使用相对路径
如果file://协议有问题，可以尝试使用相对路径：
```javascript
// 不使用file://协议，直接使用相对路径
return `<img${beforeSrc}src="${fullImagePath}"${afterSrc}>`;
```

### 方案3: Base64编码
将图片转换为Base64编码嵌入HTML中（适合小图片）。

## 测试用例

### 测试1: 基本Obsidian格式
在编辑器中输入：
```markdown
![[screenshot_20250926_091553.png]]
```

切换到预览模式，检查：
1. 控制台是否有转换日志
2. 控制台是否有路径处理日志
3. 图片是否显示

### 测试2: 标准Markdown格式
在编辑器中输入：
```markdown
![测试图片](attachments/screenshot_20250926_091553.png)
```

检查是否能正常显示。

### 测试3: 绝对路径
在编辑器中输入：
```markdown
![测试图片](E:\LLM\LectureLearnLoop\vault\attachments\screenshot_20250926_091553.png)
```

检查是否能正常显示。

## 调试工具

### 浏览器开发者工具
1. F12打开开发者工具
2. 查看Console标签页的日志
3. 查看Network标签页的资源加载情况
4. 查看Elements标签页的HTML结构

### 文件系统检查
1. 确认attachments目录存在
2. 确认图片文件存在
3. 确认文件权限正确

## 临时解决方案

如果file://协议不工作，可以尝试：

1. **使用HTTP服务器**: 启动本地HTTP服务器提供图片
2. **复制到临时目录**: 将图片复制到应用程序可访问的目录
3. **使用Base64**: 将小图片转换为Base64编码

## 下一步行动

1. 运行应用程序
2. 截图并插入文档
3. 切换到预览模式
4. 查看浏览器控制台的调试信息
5. 根据日志信息确定具体问题
6. 应用相应的解决方案
