# 学习路径功能调试指南

## 🔍 **当前状态**

### ✅ 已修复的问题
1. **知识脑图功能**：已完全恢复正常
2. **数据库语法错误**：已修复
3. **API方法存在性**：已确认存在
4. **基础功能测试**：通过

### ❓ 待调试的问题
- 点击"梳理知识点"按钮时出现错误

## 🧪 **调试步骤**

### 步骤1：重启应用
```bash
python overlay_drag_corgi_app.py
```

### 步骤2：打开浏览器开发者工具
1. 进入脑图页面
2. 按 **F12** 打开开发者工具
3. 切换到 **Console** 标签
4. 清空现有日志

### 步骤3：测试学习路径功能
1. 点击"梳理知识点"按钮
2. 观察Console中的详细日志
3. 查看是否有错误信息

### 步骤4：分析日志信息

**预期看到的正常日志**：
```
🌳 开始生成 机器学习 的学习路径图
🔍 检查WebChannel连接状态:
  - window.bridge 存在: true
  - getOrGenerateLearningPath 方法存在: true
📡 调用后端API生成学习路径...
📤 请求参数: 学科="机器学习"
📡 API响应长度: xxxx
📡 API响应预览: {"success": true, "learningPath": ...
✅ 学习路径生成成功
```

**可能的错误情况**：

#### 情况1：WebChannel连接问题
```
❌ 生成学习路径失败: WebChannel连接不可用或方法不存在
```
**解决方案**：检查WebChannel设置，重启应用

#### 情况2：API调用失败
```
📡 API响应长度: 0
📡 API响应预览: null
```
**解决方案**：检查后端API方法，查看终端日志

#### 情况3：LLM调用失败
```
📡 API响应预览: {"success": false, "error": "LLM调用失败"}
```
**解决方案**：检查LLM配置，API密钥等

#### 情况4：数据格式错误
```
❌ 生成学习路径失败: JSON解析错误
```
**解决方案**：检查API返回的数据格式

## 🔧 **可能的修复方案**

### 方案1：检查WebChannel注册
确认`getOrGenerateLearningPath`方法正确注册到WebChannel：

```python
# 在overlay_drag_corgi_app.py中检查
print(f"🔗 bridge对象方法: {[method for method in dir(self.bridge) if not method.startswith('_')]}")
```

### 方案2：简化测试
创建一个简单的测试方法，不调用LLM：

```python
@Slot(str, result=str)
def testLearningPath(self, subject_name):
    """测试学习路径功能"""
    return json.dumps({
        "success": True,
        "learningPath": {
            "data": {
                "nodes": [{"id": "test", "name": "测试节点", "type": "knowledge_point"}],
                "edges": []
            }
        }
    }, ensure_ascii=False)
```

### 方案3：检查配置
确认LLM配置正确：

```python
# 检查config中的LLM设置
print(f"LLM Provider: {self.config.get('llm_provider')}")
print(f"API Key存在: {bool(self.config.get('deepseek_api_key'))}")
```

## 📊 **调试信息收集**

请提供以下信息：

1. **浏览器Console日志**：完整的错误日志
2. **终端后端日志**：Python应用的输出
3. **具体错误信息**：错误的详细描述
4. **操作步骤**：导致错误的具体操作

## 🎯 **预期结果**

修复后应该看到：
1. ✅ 点击"梳理知识点"按钮无错误
2. ✅ 显示"生成中..."状态
3. ✅ 调用LLM生成学习路径
4. ✅ 切换到学习路径视图
5. ✅ 显示学习路径图

## 🚀 **下一步**

1. 按照调试步骤操作
2. 收集详细的错误日志
3. 根据错误类型选择对应的修复方案
4. 如果问题仍然存在，提供具体的错误信息

---

**现在请重启应用并按照调试步骤测试，然后提供具体的错误日志！** 🔍
